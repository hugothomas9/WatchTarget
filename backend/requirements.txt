requests>=2.31
beautifulsoup4>=4.12
fastapi>=0.110
uvicorn>=0.29
pytest>=8.0
playwright>=1.40
curl_cffi>=0.6
# base de données portable : SQLAlchemy (moteur) + psycopg3 (driver PostgreSQL, prod).
# En local, aucune install PG requise (SQLite intégré à Python).
SQLAlchemy>=2.0
psycopg[binary]>=3.1
# matching image (variante exacte EveryWatch) — torch CPU suffit :
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
open_clip_torch>=2.24
